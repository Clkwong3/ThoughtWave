// Import the modules
const User = require("./User"); // Import the User module.
const Thought = require("./Thought"); // Import the Thought module.

// Export both modules to use
module.exports = { User, Thought }; // Export User and Thought as an object.
